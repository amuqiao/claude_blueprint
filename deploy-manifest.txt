# Paths deployed from this repository into ~/.claude
# One path per line, relative to repo root.
#
# 说明：
# - 核心部署路径：CLAUDE.md, settings.json, commands/, skills/, agents/
# - 最小启用的可选路径：rules/（当前仅用于验证 rules 机制）
# - 自定义扩展：hooks/ (脚本统一管理), templates/ (项目初始化模板)

# ── 核心路径 ──
CLAUDE.md
settings.json
commands/
skills/
agents/

# ── 最小启用的可选路径 ──
rules/

# ── 自定义扩展 ──
hooks/
templates/
