好，结合完整对话，重新复述你的需求：

---

## 你真正要的是什么

**一份团队 onboarding 知识库**，以 Markdown 为载体，内容目标是让新成员建立完整心智模型，能独立区分和正确使用 Claude Code CLI 与 Codex CLI 的全部能力。

---

## 内容要求

**受众起点**：知道 CLAUDE.md / AGENTS.md 是"给 AI 的指令文件"，但不清楚它和其他配置文件的分工，不了解两个工具的完整能力体系。

**覆盖的能力维度**（完整列表，无遗漏）：
Instructions / Rule / Hook / Skill / Command / Plugin / MCP / Agent / Profile / Memory / Settings 配置

**四个认知任务全部覆盖**：
- 区分（概念映射，命名差异）
- 使用（操作路径，文件位置，格式示例）
- 决策（决策树：用什么？放哪层？）
- 排错（优先级规则，Trust 机制，常见陷阱）

**深度要求**：
- 两个工具并列，同等篇幅
- Level B 代码示例：完整模板，可直接 fork，含中文注释
- 知其然也知其所以然（解释为什么这样设计）
- 管理员视角覆盖个人开发者和 Tech Lead 两个角色

---

## 形式要求

**载体**：Markdown 作为 source of truth（主文档，放 repo / Notion，持续维护）

**可视化**：作用域层级图、优先级图、决策树——用 Markdown 原生方式表达（ASCII 图、表格、代码块），不依赖外部渲染

**结构**：任务导向（用户带着"我要做 X"来查，而不是从头读到尾）

**命名规范**：文件名、命令、配置字段保持原始英文（不强制翻译 `CLAUDE.md`、`name`、`description` 等），正文用中文

---
