有必要区分，但**现在这 4 份里有两份偏近，可以再收一层**。

## 结论

`docs/` 里的文档不是都同等关键，但目前方向是对的。  
更准确地说：

- **必要且关键**
  - [`docs/用户心智模型.md`](/Users/admin/Downloads/Code/claude_blueprint/docs/用户心智模型.md)
  - [`docs/能力地图.md`](/Users/admin/Downloads/Code/claude_blueprint/docs/能力地图.md)
  - [`docs/项目开发主干流程.md`](/Users/admin/Downloads/Code/claude_blueprint/docs/项目开发主干流程.md)

- **有价值，但和主干流程较近，容易重叠**
  - [`docs/项目级落地范式.md`](/Users/admin/Downloads/Code/claude_blueprint/docs/项目级落地范式.md)

- **桥接型文档，可保留，但要防重复**
  - [`docs/工作流参考.md`](/Users/admin/Downloads/Code/claude_blueprint/docs/工作流参考.md)

## 为什么有必要区分

因为这几份文档回答的问题不同：

- `用户心智模型`：**这套系统怎么理解**
- `能力地图`：**现在该调用什么**
- `项目开发主干流程`：**项目开发按什么主线推进**
- `项目级落地范式`：**为什么不能把项目起盘理解成一键生成**
- `工作流参考`：**典型场景下怎么走**

这几个问题确实不是一回事，所以“完全不区分”会重新混成一坨。

## 真正的问题不是“要不要区分”

而是：

**区分到什么程度才不过度。**

我现在的判断是：

### 应该保留的主骨架
- `用户心智模型`
- `能力地图`
- `项目开发主干流程`

这 3 份最稳，分别对应：
- 理解层
- 能力层
- 方法主干层

### 最容易重叠的
- `项目级落地范式`
- `工作流参考`

这两份要非常克制。

`项目级落地范式` 最适合保留成一篇**认知纠偏文档**，不要继续膨胀。  
`工作流参考` 最适合保留成**场景索引**，不要再去讲太多方法论。

## 是否需要写进 `MAINTAINING.md`

**需要。**

因为现在 `docs/` 已经不是随手补充说明，而是有明确分工的文档层。  
如果不写进 [`MAINTAINING.md`](/Users/admin/Downloads/Code/claude_blueprint/MAINTAINING.md)，后面最容易出现：

- 新增一篇 docs，但不知道该放哪
- 同一个问题同时写进 `PLAYBOOK`、`docs/项目开发主干流程`、`docs/工作流参考`
- `docs/` 越写越多，开始失控

## 我建议在 `MAINTAINING.md` 里明确 3 件事

### 1. `docs/` 的定位
写清楚：

- `docs/` 是**总览与桥接层**
- 不替代 `README / PLAYBOOK / WHY / MAINTAINING`
- 不写逐项说明书

### 2. 每篇 docs 的职责
建议明确成：

- `用户心智模型`：解释这套系统怎么理解
- `能力地图`：解释能力分类与边界
- `项目开发主干流程`：解释项目开发主干节点
- `项目级落地范式`：解释为什么项目起盘不能理解成一键生成
- `工作流参考`：解释常见场景的推荐走法

### 3. 新增 docs 的门槛
建议写一条规则：

**只有当现有文档无法承载，且新文档解决的是一个独立问题域，才新增新的 docs 文档。**

这样能防止 `docs/` 无限制膨胀。

## 最后建议

一句话总结：

**有必要区分，但 `docs/` 里真正的核心应收敛到“理解层 / 能力层 / 主干流程层”三根主柱，其余文档只做补充，不要继续扩张。**

如果你要，我下一步可以直接帮你把这套 `docs/` 分工和新增门槛补进 [`MAINTAINING.md`](/Users/admin/Downloads/Code/claude_blueprint/MAINTAINING.md)。