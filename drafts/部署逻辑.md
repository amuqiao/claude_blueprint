思路已经按你说的改了，核心变化是：

**不再推荐把仓库直接 clone 到 `~/.claude`。**  
现在的模型变成：

- **本项目根目录**：配置源、版本管理位置
- **`~/.claude`**：Claude Code 实际读取的目标目录

也就是说，用户应该先把本仓库 clone 到一个正常的开发目录，比如：

```bash
git clone git@github.com:amuqiao/claude_blueprint.git ~/code/claude_blueprint
cd ~/code/claude_blueprint
```

然后再从项目根目录执行脚本，把受控文件同步到 `~/.claude`。

我已经新增了两个明确脚本：

- [`scripts/backup-claude.sh`](/Users/admin/Downloads/Code/claude_blueprint/scripts/backup-claude.sh)
- [`scripts/deploy-to-claude.sh`](/Users/admin/Downloads/Code/claude_blueprint/scripts/deploy-to-claude.sh)

对应使用方式也已经写进 [`README.md`](/Users/admin/Downloads/Code/claude_blueprint/README.md)，而且是按新用户能直接照着做的形式写的：

1. **本机没有 `~/.claude`**
```bash
bash scripts/deploy-to-claude.sh --dry-run
bash scripts/deploy-to-claude.sh
```

2. **本机已有 `~/.claude`，先备份再部署**
```bash
bash scripts/backup-claude.sh --dry-run
bash scripts/backup-claude.sh
bash scripts/deploy-to-claude.sh --dry-run
bash scripts/deploy-to-claude.sh
```

3. **后续获取本仓库最新更新**
```bash
cd ~/code/claude_blueprint
git pull --ff-only origin main
bash scripts/deploy-to-claude.sh --dry-run
bash scripts/deploy-to-claude.sh
```

另外我把旧的 [`scripts/install.sh`](/Users/admin/Downloads/Code/claude_blueprint/scripts/install.sh) 和 [`scripts/adopt-existing-claude.sh`](/Users/admin/Downloads/Code/claude_blueprint/scripts/adopt-existing-claude.sh) 改成了**弃用提示**，避免继续误导用户走“直接操作 `~/.claude` 仓库”这条路。

还同步更新了 [`WHY.md`](/Users/admin/Downloads/Code/claude_blueprint/WHY.md)，把这次设计调整的原因写清楚了：`~/.claude` 会产生大量运行时数据，不适合直接当配置源仓库。

验证我已经做过：
- `bash -n scripts/backup-claude.sh`
- `bash -n scripts/deploy-to-claude.sh`
- `bash -n scripts/install.sh`
- `bash -n scripts/adopt-existing-claude.sh`

都通过了。

如果你要继续收口这套方案，下一步最值得做的是：  
把 README 顶部再加一个 **“最快上手”** 区块，只保留 3 组最常用命令，让第一次来的用户一眼就知道先 clone 到哪、再执行哪个脚本。