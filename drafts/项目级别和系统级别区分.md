理解，你的疑惑点是对的。

你不是在问“`arch` 是不是全局 agent”，而是在问：

**既然 `arch` 是系统级 agent，为什么它的 prompt 里直接写死了项目级路径？**
如果这些路径在当前项目里不存在，agent 不是会一上来就踩空吗？

答案是：

**会，所以这里应该优化。**

## 关键区分

- `arch.md` 本身是**系统级 agent**
- 但它读取的上下文里，可以包含**项目级文件**
- 问题不在“读项目级文件”这件事本身
- 问题在于：**现在写法太硬，没有处理“文件不存在”的情况**

也就是说，真正该改的不是“别读这些路径”，而是改成：

**优先读这些项目级路径；如果不存在，就降级处理。**

## 你现在的写法为什么不稳

现在是这种口吻：

1. 读 `~/.claude/skills/design-doc/SKILL.md`
2. 读项目 `CLAUDE.md`
3. 读 `docs/design/INDEX.md`
4. 读 `docs/design/架构设计方案.md`

这隐含了一个前提：

**当前项目已经按你的 blueprint 初始化完成。**

但现实里不一定：
- 有的项目还没建 `docs/design/`
- 有的项目没有 `架构设计方案.md`
- 有的项目甚至还没初始化文档体系

所以 system-level agent 直接写死读取这些路径，会让它对“半初始化项目”不够稳。

## 正确思路

应该改成这种模式：

### 固定必读
这些是 agent 自己稳定依赖的：
- `~/.claude/skills/design-doc/SKILL.md`

### 项目级优先读
这些是“如果存在就读”：
- 项目 `CLAUDE.md`
- `docs/design/INDEX.md`
- `docs/design/架构设计方案.md`

### 缺失时降级
如果不存在，就：
- 明确说明缺了什么
- 基于已有上下文继续给建议
- 同时提示需要补哪些文档

## 最适合你的写法

我建议 `agents/arch.md` 改成这种风格：

```markdown
**每次开始前，按顺序收集上下文**：

1. 必读：`~/.claude/skills/design-doc/SKILL.md`
2. 若存在，读取项目 `CLAUDE.md`
3. 若存在，读取 `docs/design/INDEX.md`
4. 若存在，读取 `docs/design/架构设计方案.md`

如果 `docs/design/INDEX.md` 或 `docs/design/架构设计方案.md` 不存在：
- 明确说明缺失了哪些项目文档
- 基于当前已知上下文继续给出架构建议
- 在“文档影响”部分提示用户补建对应文档
```

这样就对了。

## 同样的问题也存在于 `rev`

`rev` 现在也有类似问题：
- 读项目 `CLAUDE.md`
- 读 `docs/design/INDEX.md`

这里也应该改成：
- 项目 `CLAUDE.md` 优先读
- `docs/design/INDEX.md` 若存在则读
- 不存在时说明“模块边界文档缺失，审查只能基于代码结构和项目 CLAUDE.md 进行”

## 一句话总结

你的判断是对的：

**系统级 agent 可以读取项目级文件，但不能假设这些文件永远存在。**
所以这里需要的不是“去项目化”，而是**加存在性检查和降级策略**。

如果你要，我下一步可以直接帮你把 [`agents/arch.md`](/Users/admin/Downloads/Code/claude_blueprint/agents/arch.md) 和 [`agents/rev.md`](/Users/admin/Downloads/Code/claude_blueprint/agents/rev.md) 都改成这种更稳的写法。



理解，而且这次你的问题更准确了。

你真正问的是：

**当系统级 agent 依赖项目级文档时，如果这些项目级文档还不存在，应该由谁负责“先补齐项目上下文”？**

不是降级，不是跳过，而是：

- 先把项目级基础文档补起来
- 再让 `arch` / `rev` 这类系统级 agent 正常工作

这个问题本质上是：

**系统级能力如何初始化项目级上下文。**

## 先给结论

**最合适的是：用一个显式 command 负责补齐，不要靠 agent、skill、hook 或 rules。**

也就是：

- `agent` 负责分析
- `skill` 负责写法规范
- `command` 负责 orchestration（编排流程）
- `hook` 负责强制/提醒
- `rules` 负责附加约束
- `templates` 负责提供骨架

所以“项目级文档缺失时先补齐”这件事，**最适合放在 command**。

---

## 为什么不是其他机制

### 1. 不是 agent
因为 agent 的职责应该是：

- 基于已有上下文做分析
- 给判断
- 保持角色纯度

如果你让 `arch` 既负责分析架构、又负责补齐缺失文档，它就从“架构顾问”变成“初始化工作流执行器”了，角色会变脏。

更关键的是：  
补齐文档通常不只是分析，还涉及：

- 建目录
- 复制模板
- 写初始文档
- 更新索引

这已经是**工作流**，不是单一 agent 角色。

### 2. 不是 skill
因为 skill 更适合：

- 约束内容格式
- 规定写法
- 给结构模板

比如 `design-doc` 适合告诉 Claude：

- 架构文档该写什么
- 模块设计文档怎么写

但它不适合负责“判断当前项目缺什么，然后自动建什么”。

也就是说：

- skill 适合“怎么写”
- 不适合“什么时候先补齐、补哪些东西”

### 3. 不是 hook
hook 不适合做这个，因为它是：

- 强制触发
- 全局触发
- 低容错

如果你把“发现架构文档不存在就先补齐”做成 hook，会有两个问题：

- 太重：不是每次都值得打断用户
- 太冒进：用户只是问个问题，你就先改项目文档，副作用太大

hook 适合阻止危险动作，不适合主动初始化项目文档。

### 4. 不是 rules
`rules/` 只能提供补充指令，不会编排实际动作。

它能告诉模型：
- 处理某目录时要遵守什么

但它不能自然承担：
- 缺文档时先补文档
- 用模板初始化目录
- 再调用 arch 分析

所以规则不能替代初始化流程。

### 5. 不是 templates 单独承担
模板当然需要，但模板只是“静态骨架”。

模板解决的是：
- 补齐时写什么初稿

但模板本身不解决：
- 什么时候触发补齐
- 由谁执行补齐
- 补齐完怎么继续后续流程

所以模板只能是补齐流程的一部分，不是流程本身。

---

## 最合理的职责拆分

### system-level command
负责：
- 检查项目级基础文档是否存在
- 缺失则补齐
- 必要时调用 `arch` 做初步架构分析
- 再调用 `design-doc` skill 写出初稿
- 更新 `docs/design/INDEX.md`

### system-level agent `arch`
负责：
- 基于已有项目上下文做架构判断
- 如果上下文不足，指出“应该先补哪些文档”
- 参与生成架构文档的分析内容

### system-level skill `design-doc`
负责：
- 约束架构文档和模块设计文档的格式与结构

### templates
负责：
- 提供 `project-CLAUDE.md`
- 提供 `docs-INDEX.md`
- 未来可以补 `架构设计方案.md` 初始模板

---

## 你现在真正缺的是一个“项目初始化 / 架构补齐” command

我建议新增一个全局 command，名字可以是：

- `/bootstrap-project`
- `/init-docs`
- `/init-architecture`
- `/project-bootstrap`

如果按你现有风格，我更建议：

**`/init-architecture`**

因为它目标最明确：  
**当项目里还没有 `docs/design/INDEX.md` 和 `架构设计方案.md` 时，先把架构文档体系补起来。**

---

## 这个 command 应该做什么

我建议流程是这样的：

### Step 1. 检查项目级基础文件
检查是否存在：

- 项目 `CLAUDE.md`
- `docs/design/INDEX.md`
- `docs/design/架构设计方案.md`
- `docs/实施清单.md`

### Step 2. 缺失则补齐骨架
用 `templates/` + 约定结构补齐：

- 如果没有 `docs/design/INDEX.md`，从模板创建
- 如果没有 `docs/实施清单.md`，创建空文件
- 如果没有 `docs/design/架构设计方案.md`，创建架构文档初稿
- 如果没有项目 `CLAUDE.md`，提示用户填项目命令和目录映射，或者基于当前代码结构生成初稿

### Step 3. 用 `arch` 分析当前项目
这里才调用 `arch`：

- 看现有代码结构
- 判断后端/前端分层
- 给出架构文档应该包含的核心内容
- 判断哪些还属于待定

### Step 4. 用 `design-doc` skill 写架构文档
根据 `arch` 的输出，生成或补全：

- `docs/design/架构设计方案.md`

### Step 5. 更新 `INDEX.md`
把架构文档登记进去，状态设为：
- 🆕 新建中
或
- ✅ 已初始化

---

## 这样做的好处

### 对系统级 agent 更干净
`arch` 不再承担“补齐工程骨架”的职责，只负责分析。

### 对项目级上下文更稳
以后调用 `arch` / `/new-module` 之前，先跑一次 `/init-architecture`，项目上下文就完整了。

### 对用户心智更清晰
用户能理解成：

- 项目还没建文档体系 → 跑 `/init-architecture`
- 新增功能模块 → 跑 `/new-module`
- 代码落库后同步文档 → 跑 `/update-docs`

这比让 agent 自己“遇缺就补”清晰很多。

---

## 推荐你现在不要做的事

### 不要把“自动补齐”直接塞进 `arch.md`
这样会把 agent 角色搞混。

### 不要把“缺文档就补齐”写进 hooks
太重，也容易误触发。

### 不要把这件事交给 skill
skill 只能规范写法，不能承担初始化编排。

---

## 关于你提到的“系统级 vs 项目级”的真正边界

你这个问题反过来也证明了一件事：

**系统级内容可以引用项目级内容，但不能假设项目级内容天然存在。**

所以系统级 blueprint 里最好有两类 command：

### 1. 补齐类 command
用于初始化项目上下文
- `/init-architecture`
- 未来也可以有 `/init-project-docs`

### 2. 工作流类 command
在上下文已存在的前提下工作
- `/new-module`
- `/update-docs`
- `/add-rule`

你现在缺的是第一类，不是第二类。

---

## 我的建议

### 现在最值得新增的东西
**新增一个全局 command：`/init-architecture`**

它负责：
- 检查项目文档骨架
- 缺失就补齐
- 调用 `arch`
- 用 `design-doc` skill 输出 `架构设计方案.md`
- 更新 `INDEX.md`

### 同时微调 `arch.md`
不是让它自动补齐，而是把它的前置条件写清楚：

- 优先读取 `docs/design/INDEX.md`
- 优先读取 `docs/design/架构设计方案.md`
- 如果缺失，指出“应先运行 `/init-architecture` 补齐项目架构文档体系”

这样 agent 和 command 的边界就清楚了。

---

## 一句话结论

你的问题不是“系统级 agent 能不能读项目级文档”，而是：

**当项目级文档不存在时，应该由一个显式 command 先补齐项目上下文。**

所以最优解是：

- **新增 `/init-architecture` command**
- `arch` 保持分析角色
- `design-doc` 继续负责文档格式
- `templates/` 继续提供骨架
- 不用 hook / rules / skill 去硬做初始化编排

如果你要，我下一步可以直接帮你把这套东西落下来：
1. 新增 `commands/init-architecture.md`
2. 给 `templates/` 增加一个 `架构设计方案.md` 初始模板
3. 微调 `agents/arch.md` 的前置条件提示


理解。我先直接回答你最关心的点：

**这个工作流不应该自动触发。**  
至少“补齐项目级架构文档体系”这件事，不应该靠 agent 自己默默触发，也不应该靠 hook 隐式触发。

更合理的是：

- **显式触发初始化**
- **初始化完成后，后续工作流再默认依赖这些文档存在**

也就是说，应该拆成两阶段：

1. **项目文档体系初始化**
2. **基于文档体系的正常开发工作流**

---

## 我从 `title` 项目里看到的关键信号

这个项目已经非常接近你想要的“规范化开发模式”了。

### 1. 它已经有完整的项目级文档体系
你现在 `title` 里已经有这些关键文件：

- [`docs/design/INDEX.md`](/Users/admin/Downloads/Code/title/docs/design/INDEX.md)
- [`docs/design/架构设计方案.md`](/Users/admin/Downloads/Code/title/docs/design/架构设计方案.md)
- [`docs/实施清单.md`](/Users/admin/Downloads/Code/title/docs/实施清单.md)
- 大量模块文档和基础设施文档

这说明：

**`arch` 这类系统级 agent 在“成熟项目”里是成立的。**

因为它读的那些路径，在这个项目里确实存在。

### 2. 它已经呈现出你要的“目标状态”
比如：
- 架构文档
- 模块设计文档
- 专题复盘
- 需求输入
- 实施清单

这些都不是理论结构，而是已经真实跑起来了。

这很重要，因为它说明：

**你要抽象出来的 blueprint，不应该从空想开始，而应该从 `title` 这种“已经跑通的项目形态”反推。**

### 3. 但它也暴露出一个边界问题
`title` 里同时有：

- [`CLAUDE.md`](/Users/admin/Downloads/Code/title/CLAUDE.md)
- [`.claude/CLAUDE.md`](/Users/admin/Downloads/Code/title/.claude/CLAUDE.md)

而你引用的目录参考里已经明确了：

> 项目 `CLAUDE.md` 和 `.claude/CLAUDE.md` 应该二选一

这说明你现在真实项目已经出现了一个典型问题：

**项目级 Claude 配置边界还没完全收口。**

所以你的 blueprint 现在最需要做的，不是再增加更多机制，而是把：

- 全局层
- 项目层
- 初始化时机
- 正常开发时机

这几个边界钉死。

---

## 你的真正问题是什么

你真正的问题不是：

“`arch` 能不能读项目级文档？”

而是：

**系统级能力什么时候可以假设项目级上下文已经存在？**

这才是核心。

我的结论是：

### 系统级 agent 可以依赖项目级文档，但前提是：
**项目已经经过一次显式初始化。**

所以你应该明确区分：

### A. 初始化工作流
负责把项目级上下文补齐：
- 项目 `CLAUDE.md`
- `docs/design/INDEX.md`
- `docs/design/架构设计方案.md`
- `docs/实施清单.md`

### B. 正常开发工作流
假设这些上下文已经存在：
- `arch` 分析
- `/new-module`
- `/update-docs`
- `rev` 审查

---

## 所以，这个工作流该不该自动触发？

### 不该自动触发
原因很简单：

1. **会有副作用**
   初始化不是纯分析，它会创建目录、写文档、更新索引。

2. **用户意图不一定是“我要补齐体系”**
   用户可能只是问一个架构问题，不一定想你顺手改项目文件。

3. **自动触发会让系统层和项目层边界变脏**
   你会越来越难判断：
   - 这是分析
   - 还是初始化
   - 还是文档补齐
   - 还是项目治理

所以这件事不应该是自动触发。

---

## 那应该由谁负责？

### 不是 agent
因为 agent 应该保持角色纯度。

`arch` 负责分析，不负责项目初始化。

### 不是 skill
因为 skill 负责“怎么写”，不负责“什么时候先补”。

### 不是 hook
因为 hook 适合强制和提醒，不适合主动创建一套文档体系。

### 不是 rules
因为 rules 只能补指令，不能编排初始化动作。

### 最适合的是：**显式 command**
也就是新增一个全局 command，专门负责：

**初始化项目架构文档体系。**

---

## 我建议的方案

### 新增一个全局 command
名字建议：

**`/init-architecture`**

这个 command 的职责非常清楚：

1. 检查项目级基础文档是否存在
2. 缺失则补齐骨架
3. 必要时调用 `arch` 做初步架构分析
4. 用 `design-doc` skill 写出初稿
5. 更新 `docs/design/INDEX.md`

### 而 `arch` 的前提要改成：
不是“无条件去读那些路径”，而是：

- 这些路径是**正常开发状态下的标准输入**
- 如果缺失，应该提示：
  **请先运行 `/init-architecture`**

这不是降级，而是明确前置条件。

---

## 基于 `title` 项目，我建议你的标准流程这样设计

### 阶段 1：项目初始化
适用于新项目，或者旧项目还没建立文档体系。

执行：
- `/init-architecture`

产出：
- 项目 `CLAUDE.md`
- `docs/design/INDEX.md`
- `docs/design/架构设计方案.md`
- `docs/实施清单.md`

### 阶段 2：正常开发
初始化完成后，才进入标准工作流：

- 新模块：`/new-module`
- 代码后同步文档：`/update-docs`
- 新规则归档：`/add-rule`
- 架构分析：`@arch`
- 规范审查：`@rev`

这样边界就稳了。

---

## 从 `title` 得到的额外启发

### 1. 你的 blueprint 应该面向“有文档体系的项目”
因为 `title` 证明了，一旦文档体系完整，`arch` 和 `design-doc` 这条链是成立的。

### 2. blueprint 里要明确“一次性初始化”和“日常开发”是两类命令
现在你已经有后者：
- `/new-module`
- `/update-docs`
- `/add-rule`

缺的是前者：
- `/init-architecture`

### 3. `title` 项目里项目级 Claude 配置还有收口空间
你后面最好统一成一种：
- 只保留根目录 [`CLAUDE.md`](/Users/admin/Downloads/Code/title/CLAUDE.md)
- 或只保留 [`.claude/CLAUDE.md`](/Users/admin/Downloads/Code/title/.claude/CLAUDE.md)

不要同时存在。

---

## 最终建议

### 你现在最该做的，不是让工作流自动触发
而是：

1. **新增 `/init-architecture`**
2. **把它定义成所有项目的显式起点**
3. **让 `arch` / `/new-module` 都默认依赖这个初始化已完成**

### 一句话归纳
**初始化要显式，分析才可以默认。**

如果你认可这个方向，我建议下一步我直接帮你落三件事：

1. 新增 [`commands/init-architecture.md`](/Users/admin/Downloads/Code/claude_blueprint/commands/init-architecture.md)
2. 给 `templates/` 增加一个 `架构设计方案.md` 初始模板
3. 调整 [`agents/arch.md`](/Users/admin/Downloads/Code/claude_blueprint/agents/arch.md) 和 [`commands/new-module.md`](/Users/admin/Downloads/Code/claude_blueprint/commands/new-module.md) 的前置条件描述

这三件做完，你这套 blueprint 的流程边界就会清楚很多。