---
description: 新建或重命名文件时默认遵守的命名规则
---

# 文件命名规则

- 默认不要在文件名中使用空格
- 中文文件名可以保留，但词间分隔优先使用 `-`
- 避免使用 `copy`、`final`、`最新版`、`new` 这类临时词
- 同主题的 Prompt 文件统一使用 `-Prompt.md` 后缀
- 同主题的 Checklist 文件统一使用 `-Checklist.md` 后缀
- 除非用户明确要求，否则不要批量重命名已有文件
