---
paths: []
description: 正式文档输出默认遵守的最小硬约束
---

# 文档输出规则

- 正式独立 Markdown 文档必须以 `# 一级标题 + 4 行引用块` 开头；这不是建议，是固定输出契约
- 文档默认先帮助读者建立整体理解，再展开主干结构、层级关系、边界区别
- 不要堆概念；高抽象内容必须改写成更容易扫描的结构
- 复杂内容优先用文本树、对比表、结构化清单；只有再复杂时才使用 Mermaid
- 图示服务理解，不服务装饰；保持专业、克制、美观
