# {项目名} 开发规范

> 继承全局规范：`~/.claude/CLAUDE.md`（通用分层约束、通用工作流、skill 指针）
> 本文件只记录项目专属内容，不重复全局规范。

---

## 开发命令

```bash
{启动命令}     # 启动全部服务
{停止命令}     # 停止全部服务
{状态命令}     # 查看运行状态
{日志命令}     # 查看日志
```

**日志文件**（遇到问题先读这里，不询问用户）：
```
{后端日志路径}    # {说明}
{任务日志路径}    # {说明}（如有异步任务）
```

**诊断工具**（如有）：
```bash
{诊断命令 1}    # {说明}
{诊断命令 2}    # {说明}
```

---

## 目录到层级映射

后端：

| 目录 | 层级 |
|------|------|
| `{后端入口层目录}` | 入口层 |
| `{后端应用层目录}` | 应用层 |
| `{后端数据访问层目录}` | 数据访问层 |
| `{后端基础设施层目录}` | 基础设施层 |

前端：

| 目录 | 层级 |
|------|------|
| `{前端页面层目录}` | 页面层 |
| `{前端组件层目录}` | 组件层 |
| `{前端状态层目录}` | 状态层 |
| `{前端服务层目录}` | 服务层 |
| `{前端基础设施层目录}` | 基础设施层 |

---

## 项目专属约束

<!-- 只写在全局规范里找不到的内容 -->
<!-- 每条约束格式：是什么 + 为什么 + 违反会怎样 -->

{填写本项目专属的技术约束}

---

## 文档入口

`docs/design/INDEX.md`

